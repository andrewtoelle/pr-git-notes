# pr-git-notes live demo

This file exists so the demo PR has a second commit that touches a file —
giving Mode α a second commit to anchor a git-note annotation to.

Each of this PR's two commits carries a git-note (`refs/notes/commits`);
the `git-notes` check run renders them as annotations and exposes the
`ack` / `resolved` write-back buttons.
