{
	"name": "TODO-action-name",
	"version": "0.1.0",
	"description": "TODO: one-sentence description of what this action does.",
	"private": false,
	"type": "module",
	"scripts": {
		"build": "bun build src/index.ts --target node --outfile dist/index.js",
		"typecheck": "tsc --noEmit",
		"test": "bun test"
	},
	"devDependencies": {
		"@actions/core": "^3.0.1",
		"@octokit/request": "^10.0.10",
		"@types/bun": "^1.3.14",
		"typescript": "^5.0.0"
	}
}
